## 本地开发

- 更新最新代码

```shell
git pull
```

- 项目开发

- 提交代码

### 发版前准备(如果已配置请忽略)

- GitHub 仓库需要配置 `NPM_TOKEN`

```text
Settings -> Secrets and variables -> Actions -> Repository secrets -> NPM_TOKEN
```

- `NPM_TOKEN` 需要有发布 `@kunlunxu/brick` 包的权限
- 默认通过 `GITHUB_TOKEN` 推送版本提交、tag 和创建 GitHub Release，无需额外配置

## 发版流程

项目发版由 `.github/workflows/release.yml` 自动完成，主要流程如下：

1. 生成版本提交、`CHANGELOG.md` 和 git tag
2. 推送版本提交和 tag
3. 创建 GitHub Release
4. 编译 npm 包产物
5. 发布 npm 包

### 自动发版

当需要发版时，在 `master` 分支提交一个发版 commit，并 push 到远程仓库。

发版 commit message 格式必须是：

```text
release: x.y.z
```

例如：

```text
release: 9.1.2
```

只有最新 commit message 符合 `release: x.y.z` 格式时，才会触发 `Release workflow` 执行发布。普通业务提交 push 到 `master` 不会发版。

```shell
git checkout master
git pull
git commit --allow-empty -m "release: 9.1.2"
git push origin master
```

`workflow` 会执行：

```shell
npm ci
npx --no-install standard-version --release-as 9.1.2
git push origin HEAD:master --follow-tags
npm run build:publish
npm publish --access public
```

发布完成后会生成:

- `chore(release): ...` 版本提交
- `vX.Y.Z` tag
- `GitHub Release`
- `npm` 包新版本

### 补充: 版本号规则

版本号由发版 commit message 显式指定，例如：

```text
release: 9.1.2
```

workflow 会解析出 `9.1.2`，并执行：

```shell
npx --no-install standard-version --release-as 9.1.2
```

`CHANGELOG.md` 仍会根据 Conventional Commits 生成，业务提交信息建议使用：

```shell
fix: 修复 markdown 样式问题
feat: 新增 editor onCopy 配置
feat!: 调整组件导出结构
```

发版 commit 只负责指定最终版本号，不建议在该 commit 中混入业务代码。

### 手动指定版本发版

如果需要指定版本号，可以在 GitHub Actions 页面手动触发：

```text
Actions -> Release -> Run workflow
```

在 `release_as` 中填写目标版本号，例如：

```text
2.6.5
```

对应本地命令等价于：

```shell
npx --no-install standard-version --release-as 2.6.5
```

### 本地手动发版

正常情况下优先使用 GitHub Actions 发版。只有当 CI 不可用，或需要本地排查发版问题时，再使用本地手动流程。

- 拉取远程仓库数据

```shell
# 拉取远程数据包括 tag 信息
git fetch origin --prune
```

- 版本发布， 生成 changelog 和  tag

```shell
# 指定版本号
npx --no-install standard-version --release-as 2.6.5
```

```shell
# 如遇如下问题请删除 v1.1.5 分支
✔ bumping version in package.json from 1.1.6 to 1.1.6
Error in git-raw-commits: warning: refname 'v1.1.5' is ambiguous.

```

- push commit 和 tag 到远程

```shell
git push --follow-tags
```

- 创建 github releases: 直接讲 changelog 当前版本信息拷贝过去

- npm 包编译

```shell
npm run build:publish
```

- 发布包

```shell
# 1. 切换官方源
npm config set registry https://registry.npmjs.org

# 2. 登录 npm
npm login

# 3. 发布包
npm publish --access public

# 4. 如果需要则切换回淘宝源 / https://registry.yarnpkg.com
npm config set registry https://registry.npmmirror.com
```

### 发版注意事项

- `chore(release):` 提交会被 Release workflow 跳过，避免发版提交再次触发发版
- 只有 `release: x.y.z` 格式的 commit 会触发自动发版
- 如果 tag 已存在，需要先确认 npm 和 GitHub Release 是否已经发布成功，不要直接重复发布
- 如果 `npm publish` 失败，先确认 `NPM_TOKEN` 是否有效，以及当前版本是否已经存在于 npm
- 如果 GitHub Release 创建失败，可以根据 `CHANGELOG.md` 中当前版本内容手动补建 Release

```shell
npm view @kunlunxu/brick version
```
